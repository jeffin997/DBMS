DECLARE
N NUMBER(20);
I NUMBER(20);
M NUMBER(20);

BEGIN
N:=:N;
I:=2;
M:=1;

FOR I IN 2..N/2
LOOP

IF MOD (N,I)=0 THEN
M:=0;
EXIT;
END IF;
END LOOP;

IF M=1
THEN
DBMS_OUTPUT.PUT_LINE(N||' IS PRIME');
ELSE
DBMS_OUTPUT.PUT_LINE(N||' IS NOT PRIME');
END IF;
END
